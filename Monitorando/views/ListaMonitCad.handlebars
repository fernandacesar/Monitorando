
 
    <div class="painel-pesquisaMonitorias">
        <div class="Titulo margem">
            <h4 class="Descrição">Encontrando Monitorias</h4>
        </div>
        <div id="alinhar" class="Titulo margem">
            <input type="text" class="form-control" id="input-pesquisa" name="pesquisa" placeholder="Palavra - chave">
            <input class="inputDados1 btn-link" type="submit" value="Pesquisar">
        </div>

        <div class="tabela">
            <table class="bordered striped centered">
                <thead>
                    <div>
                      <tr>
                          <th>Conteúdo</th>
                          <th>Data e horário</th>
                          <th>Número de vagas</th>
                          <th>Números de inscritos</th>
                          <th>Descrição</th>
                          <th></th>
                      </tr>
                    </div>
                </thead>

                <tbody>
                    {{#each monitorias}}
                        <tr>
                            <td>{{this.conteudo}}</td>
                            <td>{{this.datahorario}}</td>
                            <td>{{this.numvagas}}</td>
                            <td>{{this.numinscritos}}</td>
                            <td>{{this.descricao}}</td>
                            <td><button class="btn btn-ver-mais">Ver mais</button><br></td>
                        </tr>
                    {{/each}}
                </tbody>
          </table>
        </div>
    </div>


